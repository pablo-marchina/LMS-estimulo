comment on function public.list_external_credential_issuers(uuid)
is 'Participant-only issuer catalog exposed through authenticated-rpc';

comment on function public.get_participant_profile_summary(uuid)
is 'Participant-only profile projection exposed through authenticated-rpc';
