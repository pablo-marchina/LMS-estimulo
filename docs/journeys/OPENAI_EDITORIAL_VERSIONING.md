# Documento substituído

O modelo de versões editoriais de jornada foi removido em 2026-08-01.

A regra vigente está documentada em [`JOURNEY_LIFECYCLE.md`](JOURNEY_LIFECYCLE.md): cada jornada é única, possui apenas os estados `draft` e `published` e pode ser editada enquanto publicada.

Este arquivo permanece temporariamente apenas para não quebrar referências externas antigas e não deve ser usado como especificação.
