version: 0.1.0
generated_at: '2026-07-08'
routes:
- event_alias: identity.invitation.created
  bounded_context: identity
  class: domain
  producer: identity
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: invitation
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'invitation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: notification_orchestrator;audit_projection
  external_effect: notification job may be created
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: invitation_id;entrepreneur_id;purpose;expires_at
  optional_data: business_id;program_id;journey_version_id
  notes: ''
- event_alias: identity.invitation.sent
  bounded_context: identity
  class: integration
  producer: notification_connector
  flow_pattern: external_delivery_callback
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: invitation
  synchronous_write_set: delivery_receipt or observation; canonical_event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'invitation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: delivery_projection
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: pseudonymous
  evidence_kind: external_confirmed
  feature_input: ineligible
  retention_class: operational
  required_data: invitation_id;delivery_id;channel;provider
  optional_data: template_version
  notes: ''
- event_alias: identity.invitation.delivered
  bounded_context: identity
  class: integration
  producer: notification_connector
  flow_pattern: external_delivery_callback
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: invitation
  synchronous_write_set: delivery_receipt or observation; canonical_event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'invitation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: delivery_projection
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: pseudonymous
  evidence_kind: external_confirmed
  feature_input: conditional
  retention_class: operational
  required_data: invitation_id;delivery_id;channel;delivered_at
  optional_data: ''
  notes: ''
- event_alias: identity.invitation.opened
  bounded_context: identity
  class: behavioral
  producer: notification_connector
  flow_pattern: external_delivery_callback
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: invitation
  synchronous_write_set: delivery_receipt or observation; canonical_event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'invitation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: engagement_projection
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: pseudonymous
  evidence_kind: external_observed
  feature_input: weak_only
  retention_class: operational
  required_data: invitation_id;delivery_id;channel
  optional_data: client_time
  notes: Não usar isoladamente; abertura pode ser bloqueada ou simulada pelo provedor.
- event_alias: identity.account.registered
  bounded_context: identity
  class: domain
  producer: identity
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: user_account
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'user_account: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: identity_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: user_account_id;entrepreneur_id;registration_method
  optional_data: ''
  notes: ''
- event_alias: identity.email.verified
  bounded_context: identity
  class: domain
  producer: identity
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: user_account
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'user_account: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: identity_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: user_account_id;verified_at
  optional_data: ''
  notes: ''
- event_alias: identity.account.activated
  bounded_context: identity
  class: domain
  producer: identity
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: user_account
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'user_account: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: identity_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: user_account_id;entrepreneur_id
  optional_data: ''
  notes: ''
- event_alias: identity.account.suspended
  bounded_context: identity
  class: audit
  producer: identity
  flow_pattern: privileged_transaction
  authoritative_source: platform_domain_transaction
  aggregate: user_account
  synchronous_write_set: domain/audit state; canonical_event; outbox; audit metadata
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'user_account: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: authorization_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: user_account_id;reason_code;suspended_by
  optional_data: expires_at
  notes: ''
- event_alias: identity.authentication.succeeded
  bounded_context: identity
  class: audit
  producer: identity
  flow_pattern: privileged_transaction
  authoritative_source: platform_domain_transaction
  aggregate: authentication
  synchronous_write_set: domain/audit state; canonical_event; outbox; audit metadata
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: received_at; no behavioral ordering guarantee
  consumers: security_monitoring
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_acknowledged
  feature_input: ineligible
  retention_class: security_short
  required_data: user_account_id;authentication_method
  optional_data: session_id;risk_signals
  notes: ''
- event_alias: identity.authentication.failed
  bounded_context: identity
  class: audit
  producer: identity
  flow_pattern: privileged_transaction
  authoritative_source: platform_domain_transaction
  aggregate: authentication
  synchronous_write_set: domain/audit state; canonical_event; outbox; audit metadata
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: received_at; no behavioral ordering guarantee
  consumers: security_monitoring
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_acknowledged
  feature_input: ineligible
  retention_class: security_short
  required_data: principal_hash;reason_code
  optional_data: risk_signals
  notes: ''
- event_alias: governance.consent.granted
  bounded_context: governance
  class: domain
  producer: governance
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: consent
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'consent: aggregate_id + aggregate_version when stateful; otherwise event_id/received_at'
  consumers: consent_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: consent_record_id;entrepreneur_id;purpose_code;notice_version;granted_at
  optional_data: expires_at
  notes: ''
- event_alias: governance.consent.withdrawn
  bounded_context: governance
  class: domain
  producer: governance
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: consent
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'consent: aggregate_id + aggregate_version when stateful; otherwise event_id/received_at'
  consumers: consent_projection;intervention_orchestrator;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: consent_record_id;entrepreneur_id;purpose_code;withdrawn_at
  optional_data: reason_code
  notes: ''
- event_alias: catalog.journey_version.published
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: journey_definition_id;journey_version_id;version;published_at
  optional_data: publisher_organization_id
  notes: ''
- event_alias: catalog.journey_version.retired
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: journey_definition_id;journey_version_id;retired_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: catalog.course_version.published
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: course_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'course_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: course_definition_id;course_version_id;version;published_at
  optional_data: publisher_organization_id
  notes: ''
- event_alias: catalog.course_version.retired
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: course_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'course_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: course_definition_id;course_version_id;retired_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: catalog.activity_version.published
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: activity_definition_id;activity_version_id;version;published_at
  optional_data: publisher_organization_id
  notes: ''
- event_alias: catalog.activity_version.retired
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: activity_definition_id;activity_version_id;retired_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: catalog.diagnostic_version.published
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: diagnostic_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: diagnostic_definition_id;diagnostic_version_id;version;published_at
  optional_data: publisher_organization_id
  notes: ''
- event_alias: catalog.diagnostic_version.retired
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: diagnostic_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: diagnostic_definition_id;diagnostic_version_id;retired_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: catalog.assessment_version.published
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: assessment_definition_id;assessment_version_id;version;published_at
  optional_data: publisher_organization_id
  notes: ''
- event_alias: catalog.assessment_version.retired
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_version: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: assessment_definition_id;assessment_version_id;retired_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: catalog.intervention_version.published
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_version: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: intervention_definition_id;intervention_version_id;version;published_at
  optional_data: publisher_organization_id
  notes: ''
- event_alias: catalog.intervention_version.retired
  bounded_context: learning_catalog
  class: domain
  producer: catalog
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_version
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_version: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: catalog_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: intervention_definition_id;intervention_version_id;retired_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: journey.enrollment.created
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: enrollment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'enrollment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;crm_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: enrollment_id;entrepreneur_id;business_id;journey_version_id;program_id;source
  optional_data: cohort_id;eligible_from;expires_at
  notes: ''
- event_alias: journey.enrollment.activated
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: enrollment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'enrollment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: enrollment_id;journey_instance_id;activated_at
  optional_data: ''
  notes: ''
- event_alias: journey.enrollment.cancelled
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: enrollment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'enrollment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;intervention_orchestrator;crm_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: enrollment_id;reason_code;cancelled_at
  optional_data: cancelled_by
  notes: ''
- event_alias: journey.enrollment.expired
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: enrollment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'enrollment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: enrollment_id;expired_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: journey.instance.available
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: journey_instance_id;journey_version_id;available_at
  optional_data: expires_at
  notes: ''
- event_alias: journey.instance.started
  bounded_context: journey_orchestration
  class: behavioral_domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;behavioral_store;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: journey_instance_id;started_at;entry_point
  optional_data: ''
  notes: ''
- event_alias: journey.instance.paused
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: journey_instance_id;reason_code;paused_at
  optional_data: ''
  notes: ''
- event_alias: journey.instance.resumed
  bounded_context: journey_orchestration
  class: behavioral_domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: journey_instance_id;resumed_at
  optional_data: pause_duration_seconds
  notes: ''
- event_alias: journey.milestone.achieved
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;credential_engine;crm_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: derived_only
  retention_class: operational
  required_data: journey_instance_id;milestone_definition_id;milestone_code;achieved_at;evidence_refs
  optional_data: ''
  notes: ''
- event_alias: journey.instance.completed
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: journey_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'journey_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;credential_engine;crm_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: derived_only
  retention_class: operational
  required_data: journey_instance_id;completion_policy_version;completed_at;evidence_refs
  optional_data: completion_level
  notes: ''
- event_alias: journey.path.assigned
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: path_assignment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'path_assignment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;explanation_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: path_assignment_id;journey_instance_id;path_template_id;assignment_policy_version;reason_codes
  optional_data: diagnostic_result_id;segment_assignment_ids
  notes: ''
- event_alias: journey.path.reassigned
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: path_assignment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'path_assignment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: path_assignment_id;previous_path_template_id;new_path_template_id;reason_codes;reassigned_at
  optional_data: actor_override_id
  notes: ''
- event_alias: journey.path.started
  bounded_context: journey_orchestration
  class: behavioral_domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: path_assignment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'path_assignment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: path_assignment_id;journey_instance_id;started_at
  optional_data: ''
  notes: ''
- event_alias: journey.path.completed
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: path_assignment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'path_assignment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;credential_engine
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: derived_only
  retention_class: operational
  required_data: path_assignment_id;completion_policy_version;completed_at;evidence_refs
  optional_data: ''
  notes: ''
- event_alias: journey.step.available
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: activity_instance_id;journey_instance_id;path_step_id;activity_version_id;available_at
  optional_data: due_at
  notes: ''
- event_alias: journey.step.blocked
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;explanation_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: activity_instance_id;rule_version;reason_codes;blocked_at
  optional_data: ''
  notes: ''
- event_alias: journey.step.unblocked
  bounded_context: journey_orchestration
  class: domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: activity_instance_id;rule_version;evidence_refs;unblocked_at
  optional_data: ''
  notes: ''
- event_alias: journey.step.skipped
  bounded_context: journey_orchestration
  class: behavioral_domain
  producer: journey_orchestration
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: conditional
  retention_class: operational
  required_data: activity_instance_id;skip_policy_version;skipped_at
  optional_data: reason_code;participant_choice
  notes: ''
- event_alias: diagnostic.session.started
  bounded_context: diagnostics
  class: behavioral_domain
  producer: diagnostics
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: diagnostic_session
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_session: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: diagnostic_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: diagnostic_session_id;diagnostic_version_id;entrepreneur_id;started_at
  optional_data: journey_instance_id
  notes: ''
- event_alias: diagnostic.response.recorded
  bounded_context: diagnostics
  class: behavioral
  producer: diagnostics
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: diagnostic_session
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_session: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: diagnostic_projection;behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_acknowledged
  feature_input: conditional
  retention_class: operational
  required_data: diagnostic_session_id;item_version_id;response_value;response_sequence;answered_at
  optional_data: response_time_ms;client_event_id
  notes: Resposta pode ser sensível; payload deve usar códigos, nunca texto livre
    desnecessário.
- event_alias: diagnostic.response.changed
  bounded_context: diagnostics
  class: behavioral
  producer: diagnostics
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: diagnostic_session
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_session: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: diagnostic_projection;behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_acknowledged
  feature_input: conditional
  retention_class: operational
  required_data: diagnostic_session_id;item_version_id;previous_response_hash;response_value;change_sequence
  optional_data: response_time_ms
  notes: ''
- event_alias: diagnostic.session.abandoned
  bounded_context: diagnostics
  class: behavioral_domain
  producer: diagnostics
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: diagnostic_session
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_session: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: diagnostic_projection;intervention_orchestrator;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: diagnostic_session_id;abandoned_at;last_item_version_id
  optional_data: reason_code
  notes: ''
- event_alias: diagnostic.session.resumed
  bounded_context: diagnostics
  class: behavioral_domain
  producer: diagnostics
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: diagnostic_session
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_session: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: diagnostic_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: diagnostic_session_id;resumed_at
  optional_data: elapsed_seconds
  notes: ''
- event_alias: diagnostic.session.completed
  bounded_context: diagnostics
  class: behavioral_domain
  producer: diagnostics
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: diagnostic_session
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_session: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: diagnostic_projection;personalization_engine;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: diagnostic_session_id;completed_at;completeness_ratio
  optional_data: quality_flags
  notes: ''
- event_alias: diagnostic.result.generated
  bounded_context: diagnostics
  class: domain
  producer: diagnostics
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: diagnostic_result
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'diagnostic_result: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: personalization_engine;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: derived_only
  retention_class: operational
  required_data: diagnostic_result_id;diagnostic_session_id;instrument_version;dimension_results;data_quality;generated_at
  optional_data: operational_readiness
  notes: ''
- event_alias: personalization.recommendation.generated
  bounded_context: diagnostics
  class: domain
  producer: personalization_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: recommendation
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'recommendation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_orchestration;explanation_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: recommendation_id;entrepreneur_id;policy_version;recommended_start;reason_codes;generated_at
  optional_data: journey_instance_id
  notes: ''
- event_alias: personalization.recommendation.presented
  bounded_context: diagnostics
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: recommendation
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'recommendation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: weak_only
  retention_class: operational
  required_data: recommendation_id;presentation_version;presented_at
  optional_data: channel
  notes: ''
- event_alias: personalization.recommendation.accepted
  bounded_context: diagnostics
  class: behavioral_domain
  producer: application_backend
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: recommendation
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'recommendation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_orchestration;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: recommendation_id;accepted_at
  optional_data: selected_option
  notes: ''
- event_alias: personalization.recommendation.overridden
  bounded_context: diagnostics
  class: behavioral_domain
  producer: application_backend
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: recommendation
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'recommendation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_orchestration;behavioral_store;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: recommendation_id;alternative_selected;overridden_at
  optional_data: reason_code
  notes: ''
- event_alias: personalization.segment.assigned
  bounded_context: diagnostics
  class: domain
  producer: personalization_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: segment_assignment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'segment_assignment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_orchestration;intervention_orchestrator;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: segment_assignment_id;segment_definition_id;policy_version;subject_id;reason_codes;valid_from
  optional_data: valid_until;source_event_ids
  notes: ''
- event_alias: personalization.segment.removed
  bounded_context: diagnostics
  class: domain
  producer: personalization_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: segment_assignment
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'segment_assignment: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_orchestration;intervention_orchestrator;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: segment_assignment_id;removed_at;reason_code
  optional_data: ''
  notes: ''
- event_alias: personalization.uncertainty.recorded
  bounded_context: diagnostics
  class: domain
  producer: personalization_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: recommendation
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'recommendation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: research_projection;explanation_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: subject_id;policy_version;uncertainty_reasons;recorded_at
  optional_data: missing_inputs
  notes: ''
- event_alias: learning.activity.started
  bounded_context: learning_delivery
  class: behavioral_domain
  producer: learning_delivery
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: progress_projection;behavioral_store;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: activity_instance_id;activity_version_id;journey_instance_id;started_at
  optional_data: entry_point;attempt_number
  notes: ''
- event_alias: learning.activity.paused
  bounded_context: learning_delivery
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: activity_instance
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: progress_projection;behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: conditional
  retention_class: operational
  required_data: activity_instance_id;paused_at
  optional_data: position;active_duration_seconds;client_event_id
  notes: ''
- event_alias: learning.activity.resumed
  bounded_context: learning_delivery
  class: behavioral_domain
  producer: learning_delivery
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: progress_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: activity_instance_id;resumed_at
  optional_data: resume_position
  notes: ''
- event_alias: learning.activity.progressed
  bounded_context: learning_delivery
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: activity_instance
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: progress_projection;behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: conditional
  retention_class: operational
  required_data: activity_instance_id;progress_measure;progress_unit;client_event_id
  optional_data: active_duration_seconds;client_time;media_position
  notes: Eventos de progresso devem ser limitados por frequência e consolidados para
    evitar ruído.
- event_alias: learning.activity.completed
  bounded_context: learning_delivery
  class: behavioral_domain
  producer: learning_delivery
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: activity_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: progress_projection;journey_orchestration;behavioral_store;gamification_engine
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: rebuild projection from canonical events and compare with operational
    state
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: activity_instance_id;completion_policy_version;completed_at;evidence_refs
  optional_data: active_duration_seconds
  notes: ''
- event_alias: learning.activity.revisited
  bounded_context: learning_delivery
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: activity_instance
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'activity_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: conditional
  retention_class: operational
  required_data: activity_instance_id;revisited_at;client_event_id
  optional_data: entry_point
  notes: ''
- event_alias: learning.asset.opened
  bounded_context: learning_delivery
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: content_asset
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'content_asset: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: weak_only
  retention_class: operational
  required_data: content_asset_id;activity_instance_id;client_event_id;opened_at
  optional_data: asset_type
  notes: ''
- event_alias: learning.asset.downloaded
  bounded_context: learning_delivery
  class: behavioral_domain
  producer: application_backend
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: content_asset
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'content_asset: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: conditional
  retention_class: operational
  required_data: content_asset_id;activity_instance_id;download_receipt_id;downloaded_at
  optional_data: ''
  notes: ''
- event_alias: learning.external_resource.opened
  bounded_context: learning_delivery
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: external_resource
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'external_resource: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: weak_only
  retention_class: operational
  required_data: resource_definition_id;activity_instance_id;client_event_id;opened_at
  optional_data: destination_domain
  notes: Não armazenar URL completa com query string.
- event_alias: assessment.attempt.started
  bounded_context: assessment
  class: behavioral_domain
  producer: assessment
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: assessment_attempt_id;assessment_version_id;activity_instance_id;attempt_number;started_at
  optional_data: ''
  notes: ''
- event_alias: assessment.answer.recorded
  bounded_context: assessment
  class: behavioral
  producer: assessment
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: assessment_attempt
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_acknowledged
  feature_input: conditional
  retention_class: operational
  required_data: assessment_attempt_id;question_version_id;answer_payload;answer_sequence;recorded_at
  optional_data: response_time_ms
  notes: ''
- event_alias: assessment.attempt.submitted
  bounded_context: assessment
  class: behavioral_domain
  producer: assessment
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;scoring_engine;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: assessment_attempt_id;submitted_at;answered_count
  optional_data: active_duration_seconds
  notes: ''
- event_alias: assessment.attempt.scored
  bounded_context: assessment
  class: domain
  producer: scoring_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;journey_orchestration;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: derived_only
  retention_class: operational
  required_data: assessment_attempt_id;scoring_policy_version;score;max_score;scored_at
  optional_data: dimension_scores;quality_flags
  notes: ''
- event_alias: assessment.attempt.passed
  bounded_context: assessment
  class: domain
  producer: scoring_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;journey_orchestration;gamification_engine
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: derived_only
  retention_class: operational
  required_data: assessment_attempt_id;passing_policy_version;passed_at;evidence_refs
  optional_data: ''
  notes: ''
- event_alias: assessment.attempt.failed
  bounded_context: assessment
  class: domain
  producer: scoring_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: derived_only
  retention_class: operational
  required_data: assessment_attempt_id;passing_policy_version;failed_at
  optional_data: reason_codes
  notes: ''
- event_alias: assessment.attempt.invalidated
  bounded_context: assessment
  class: audit
  producer: assessment
  flow_pattern: privileged_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain/audit state; canonical_event; outbox; audit metadata
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: assessment_attempt_id;invalidated_at;reason_code;invalidated_by
  optional_data: ''
  notes: ''
- event_alias: assessment.feedback.available
  bounded_context: assessment
  class: domain
  producer: assessment
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;notification_orchestrator
  external_effect: notification job may be created
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: assessment_attempt_id;feedback_version;available_at
  optional_data: ''
  notes: ''
- event_alias: assessment.feedback.viewed
  bounded_context: assessment
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: assessment_attempt
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: conditional
  retention_class: operational
  required_data: assessment_attempt_id;feedback_version;client_event_id;viewed_at
  optional_data: ''
  notes: ''
- event_alias: assessment.retry.available
  bounded_context: assessment
  class: domain
  producer: assessment
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: assessment_attempt
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'assessment_attempt: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: assessment_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: assessment_attempt_id;retry_policy_version;next_attempt_number;available_at
  optional_data: available_until
  notes: ''
- event_alias: practice.activity.started
  bounded_context: practice
  class: behavioral_domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_instance: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: practice_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: practice_instance_id;activity_instance_id;started_at
  optional_data: ''
  notes: ''
- event_alias: practice.submission.draft_saved
  bounded_context: practice
  class: behavioral_domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: conditional
  retention_class: operational
  required_data: submission_id;practice_instance_id;revision_number;saved_at
  optional_data: field_presence;attachment_count
  notes: ''
- event_alias: practice.evidence.attached
  bounded_context: practice
  class: behavioral_domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection;security_scanner
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: conditional
  retention_class: operational
  required_data: submission_id;evidence_asset_id;asset_type;attached_at
  optional_data: content_hash
  notes: ''
- event_alias: practice.evidence.removed
  bounded_context: practice
  class: behavioral_domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: submission_id;evidence_asset_id;removed_at
  optional_data: reason_code
  notes: ''
- event_alias: practice.submission.submitted
  bounded_context: practice
  class: behavioral_domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection;review_queue;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: submission_id;practice_instance_id;revision_number;submitted_at;evidence_refs
  optional_data: self_report_fields
  notes: ''
- event_alias: practice.review.started
  bounded_context: practice
  class: audit
  producer: practice
  flow_pattern: privileged_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_review
  synchronous_write_set: domain/audit state; canonical_event; outbox; audit metadata
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_review: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: practice_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: review_id;submission_id;reviewer_id;rubric_version;started_at
  optional_data: ''
  notes: ''
- event_alias: practice.revision.requested
  bounded_context: practice
  class: domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection;notification_orchestrator;behavioral_store
  external_effect: notification job may be created
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: conditional
  retention_class: operational
  required_data: review_id;submission_id;rubric_version;reason_codes;requested_at
  optional_data: feedback_ref
  notes: ''
- event_alias: practice.submission.resubmitted
  bounded_context: practice
  class: behavioral_domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection;review_queue;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: submission_id;revision_number;resubmitted_at;evidence_refs
  optional_data: ''
  notes: ''
- event_alias: practice.submission.accepted
  bounded_context: practice
  class: domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection;journey_orchestration;gamification_engine;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: review_id;submission_id;rubric_version;accepted_at;evidence_summary
  optional_data: ''
  notes: ''
- event_alias: practice.submission.rejected
  bounded_context: practice
  class: domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: practice_submission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'practice_submission: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: practice_projection;intervention_orchestrator
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: review_id;submission_id;rubric_version;rejected_at;reason_codes
  optional_data: ''
  notes: ''
- event_alias: practice.application.self_reported
  bounded_context: practice
  class: behavioral
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: application_evidence
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'application_evidence: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: research_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: self_reported
  feature_input: conditional
  retention_class: operational
  required_data: practice_instance_id;application_status;reported_at
  optional_data: result_description_code;evidence_asset_id
  notes: Nunca equivale automaticamente a aplicação comprovada.
- event_alias: practice.application.verified
  bounded_context: practice
  class: domain
  producer: practice
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: application_evidence
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'application_evidence: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: research_projection;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: reviewer_validated
  feature_input: eligible
  retention_class: operational
  required_data: application_evidence_id;practice_instance_id;verification_method;verified_at;verifier_id
  optional_data: evidence_refs
  notes: ''
- event_alias: practice.use_permission.granted
  bounded_context: governance
  class: domain
  producer: governance
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: content_permission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'content_permission: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: content_curation;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: permission_id;submission_id;purpose_code;notice_version;granted_at
  optional_data: expires_at
  notes: ''
- event_alias: practice.use_permission.withdrawn
  bounded_context: governance
  class: domain
  producer: governance
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: content_permission
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'content_permission: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: content_curation;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: permission_id;submission_id;withdrawn_at
  optional_data: ''
  notes: ''
- event_alias: engagement.points.awarded
  bounded_context: engagement
  class: domain
  producer: gamification_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: point_ledger_entry
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'point_ledger_entry: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: points_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: verify unique source_event/rule and ledger/snapshot consistency
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: ledger_entry_id;entrepreneur_id;rule_version;points;source_event_ids;awarded_at
  optional_data: journey_instance_id
  notes: Evento derivado de fatos de origem; não usar diretamente como feature.
- event_alias: engagement.points.reversed
  bounded_context: engagement
  class: domain
  producer: gamification_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: point_ledger_entry
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'point_ledger_entry: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: points_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: verify unique source_event/rule and ledger/snapshot consistency
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: ledger_entry_id;reverses_entry_id;reason_code;reversed_at
  optional_data: ''
  notes: ''
- event_alias: engagement.badge.awarded
  bounded_context: engagement
  class: domain
  producer: gamification_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: badge_award
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'badge_award: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: badge_projection;crm_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: verify unique source_event/rule and ledger/snapshot consistency
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: badge_award_id;badge_version_id;entrepreneur_id;source_event_ids;awarded_at
  optional_data: journey_instance_id
  notes: ''
- event_alias: engagement.badge.revoked
  bounded_context: engagement
  class: domain
  producer: gamification_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: badge_award
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'badge_award: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: badge_projection;crm_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: verify unique source_event/rule and ledger/snapshot consistency
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: badge_award_id;reason_code;revoked_at
  optional_data: ''
  notes: ''
- event_alias: credential.certificate.issued
  bounded_context: credentials
  class: domain
  producer: credential_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: certificate
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'certificate: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: credential_projection;crm_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: verify unique source_event/rule and ledger/snapshot consistency
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: certificate_id;certificate_definition_version_id;entrepreneur_id;journey_instance_id;evidence_snapshot;issued_at
  optional_data: expires_at
  notes: ''
- event_alias: credential.certificate.revoked
  bounded_context: credentials
  class: domain
  producer: credential_engine
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: certificate
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'certificate: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: credential_projection;crm_projection;audit_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: verify unique source_event/rule and ledger/snapshot consistency
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: certificate_id;reason_code;revoked_at
  optional_data: ''
  notes: ''
- event_alias: credential.certificate.downloaded
  bounded_context: credentials
  class: behavioral
  producer: application_backend
  flow_pattern: client_or_external_observation
  authoritative_source: accepted_observation; not authoritative for critical domain
    state
  aggregate: certificate
  synchronous_write_set: accepted_observation/event_log; outbox; optional projection
    update
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'certificate: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: behavioral_store
  external_effect: none by default
  retry_policy: client may retry with same client_event_id; server deduplicates
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: verify unique source_event/rule and ledger/snapshot consistency
  privacy_class: pseudonymous
  evidence_kind: server_acknowledged
  feature_input: weak_only
  retention_class: operational
  required_data: certificate_id;client_event_id;downloaded_at
  optional_data: ''
  notes: ''
- event_alias: intervention.eligibility.detected
  bounded_context: interventions
  class: domain
  producer: intervention_orchestrator
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_candidate
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_candidate: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: candidate_id;intervention_definition_version_id;subject_id;trigger_event_ids;rule_version;detected_at
  optional_data: priority
  notes: ''
- event_alias: intervention.instance.created
  bounded_context: interventions
  class: domain
  producer: intervention_orchestrator
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection;notification_orchestrator
  external_effect: notification job may be created
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: intervention_instance_id;candidate_id;channel;created_at
  optional_data: scheduled_for
  notes: ''
- event_alias: intervention.instance.suppressed
  bounded_context: interventions
  class: domain
  producer: intervention_orchestrator
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: intervention_instance_id;suppression_policy_version;reason_codes;suppressed_at
  optional_data: ''
  notes: ''
- event_alias: intervention.message.sent
  bounded_context: interventions
  class: integration
  producer: notification_connector
  flow_pattern: external_delivery_callback
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: intervention_instance
  synchronous_write_set: delivery_receipt or observation; canonical_event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: pseudonymous
  evidence_kind: external_confirmed
  feature_input: ineligible
  retention_class: operational
  required_data: intervention_instance_id;delivery_id;channel;provider;sent_at
  optional_data: template_version
  notes: ''
- event_alias: intervention.message.delivered
  bounded_context: interventions
  class: integration
  producer: notification_connector
  flow_pattern: external_delivery_callback
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: intervention_instance
  synchronous_write_set: delivery_receipt or observation; canonical_event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection;research_projection
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: pseudonymous
  evidence_kind: external_confirmed
  feature_input: conditional
  retention_class: operational
  required_data: intervention_instance_id;delivery_id;delivered_at
  optional_data: ''
  notes: ''
- event_alias: intervention.message.opened
  bounded_context: interventions
  class: behavioral
  producer: notification_connector
  flow_pattern: external_delivery_callback
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: intervention_instance
  synchronous_write_set: delivery_receipt or observation; canonical_event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection;research_projection
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: pseudonymous
  evidence_kind: external_observed
  feature_input: weak_only
  retention_class: operational
  required_data: intervention_instance_id;delivery_id;opened_at
  optional_data: ''
  notes: ''
- event_alias: intervention.action.taken
  bounded_context: interventions
  class: behavioral_domain
  producer: application_backend
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection;behavioral_store;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: conditional
  retention_class: operational
  required_data: intervention_instance_id;action_code;source_event_id;acted_at
  optional_data: ''
  notes: Evitar circularidade quando a própria resposta for o outcome da intervenção.
- event_alias: intervention.instance.dismissed
  bounded_context: interventions
  class: behavioral_domain
  producer: application_backend
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: conditional
  retention_class: operational
  required_data: intervention_instance_id;dismissed_at
  optional_data: reason_code
  notes: ''
- event_alias: intervention.delivery.failed
  bounded_context: interventions
  class: integration
  producer: notification_connector
  flow_pattern: external_delivery_callback
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: intervention_instance
  synchronous_write_set: delivery_receipt or observation; canonical_event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection;operations_monitoring
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: internal
  evidence_kind: external_confirmed
  feature_input: ineligible
  retention_class: operational
  required_data: intervention_instance_id;delivery_id;failure_code;failed_at
  optional_data: retryable
  notes: ''
- event_alias: intervention.instance.cancelled
  bounded_context: interventions
  class: domain
  producer: intervention_orchestrator
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: intervention_instance
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'intervention_instance: aggregate_id + aggregate_version when stateful;
    otherwise event_id/received_at'
  consumers: intervention_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: pseudonymous
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: intervention_instance_id;reason_code;cancelled_at
  optional_data: ''
  notes: ''
- event_alias: support.requested
  bounded_context: interventions
  class: behavioral_domain
  producer: application_backend
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: support_request
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'support_request: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: support_queue;behavioral_store
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: eligible
  retention_class: operational
  required_data: support_request_id;entrepreneur_id;request_category;requested_at
  optional_data: journey_instance_id;activity_instance_id;preferred_channel
  notes: ''
- event_alias: support.request.fulfilled
  bounded_context: interventions
  class: domain
  producer: support_operations
  flow_pattern: internal_domain_transaction
  authoritative_source: platform_domain_transaction
  aggregate: support_request
  synchronous_write_set: domain state; canonical_event; outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'support_request: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: support_projection;research_projection
  external_effect: none by default
  retry_policy: command retry only with idempotency key; async consumer retries independently
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: consumer checkpoint, aggregate version and projection consistency
    checks
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: support_request_id;fulfillment_type;fulfilled_at
  optional_data: resolution_code
  notes: ''
- event_alias: integration.sync.requested
  bounded_context: integrations
  class: integration
  producer: integration_orchestrator
  flow_pattern: external_effect_async
  authoritative_source: platform_domain_transaction
  aggregate: sync_operation
  synchronous_write_set: integration_job/history; canonical integration event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'sync_operation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: integration_worker;operations_monitoring
  external_effect: external synchronization job
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: sync_operation_id;connector;entity_type;entity_id;mapping_version;requested_at
  optional_data: trigger_event_ids
  notes: ''
- event_alias: integration.sync.succeeded
  bounded_context: integrations
  class: integration
  producer: integration_worker
  flow_pattern: external_effect_async
  authoritative_source: platform_domain_transaction
  aggregate: sync_operation
  synchronous_write_set: integration_job/history; canonical integration event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'sync_operation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: integration_projection;operations_monitoring
  external_effect: external synchronization job
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: internal
  evidence_kind: external_confirmed
  feature_input: ineligible
  retention_class: operational
  required_data: sync_operation_id;connector;external_object_id;succeeded_at
  optional_data: external_version
  notes: ''
- event_alias: integration.sync.failed
  bounded_context: integrations
  class: integration
  producer: integration_worker
  flow_pattern: external_effect_async
  authoritative_source: platform_domain_transaction
  aggregate: sync_operation
  synchronous_write_set: integration_job/history; canonical integration event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'sync_operation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: integration_projection;retry_scheduler;operations_monitoring
  external_effect: external synchronization job
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: internal
  evidence_kind: external_confirmed
  feature_input: ineligible
  retention_class: operational
  required_data: sync_operation_id;connector;failure_code;failed_at;retryable
  optional_data: provider_request_id
  notes: ''
- event_alias: integration.conflict.detected
  bounded_context: integrations
  class: integration
  producer: integration_worker
  flow_pattern: external_effect_async
  authoritative_source: platform_domain_transaction
  aggregate: sync_operation
  synchronous_write_set: integration_job/history; canonical integration event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'sync_operation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: reconciliation_queue;operations_monitoring
  external_effect: none by default
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: sync_operation_id;entity_type;entity_id;conflicting_fields;detected_at
  optional_data: external_snapshot_hash
  notes: ''
- event_alias: integration.conflict.reconciled
  bounded_context: integrations
  class: integration
  producer: integration_worker
  flow_pattern: external_effect_async
  authoritative_source: platform_domain_transaction
  aggregate: sync_operation
  synchronous_write_set: integration_job/history; canonical integration event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'sync_operation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: integration_projection;audit_projection
  external_effect: none by default
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: restricted
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: sync_operation_id;resolution_policy;resolved_fields;reconciled_at
  optional_data: resolved_by
  notes: ''
- event_alias: integration.webhook.received
  bounded_context: integrations
  class: integration
  producer: webhook_gateway
  flow_pattern: external_webhook_ingestion
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: webhook_delivery
  synchronous_write_set: webhook_receipt; validation_result; normalized_event/outbox
    when valid
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'webhook_delivery: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: webhook_validation;operations_monitoring
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: internal
  evidence_kind: external_observed
  feature_input: ineligible
  retention_class: integration_short
  required_data: webhook_delivery_id;connector;provider_event_id;received_at;payload_hash
  optional_data: ''
  notes: ''
- event_alias: integration.webhook.validated
  bounded_context: integrations
  class: integration
  producer: webhook_gateway
  flow_pattern: external_webhook_ingestion
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: webhook_delivery
  synchronous_write_set: webhook_receipt; validation_result; normalized_event/outbox
    when valid
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'webhook_delivery: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: webhook_processing
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: internal
  evidence_kind: server_acknowledged
  feature_input: ineligible
  retention_class: operational
  required_data: webhook_delivery_id;signature_version;validated_at
  optional_data: ''
  notes: ''
- event_alias: integration.webhook.rejected
  bounded_context: integrations
  class: audit
  producer: webhook_gateway
  flow_pattern: external_webhook_ingestion
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: webhook_delivery
  synchronous_write_set: webhook_receipt; validation_result; normalized_event/outbox
    when valid
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'webhook_delivery: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: security_monitoring;operations_monitoring
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: restricted
  evidence_kind: server_acknowledged
  feature_input: ineligible
  retention_class: security_short
  required_data: webhook_delivery_id;reason_code;rejected_at
  optional_data: ''
  notes: ''
- event_alias: integration.webhook.processed
  bounded_context: integrations
  class: integration
  producer: integration_worker
  flow_pattern: external_effect_async
  authoritative_source: platform_domain_transaction
  aggregate: webhook_delivery
  synchronous_write_set: integration_job/history; canonical integration event/outbox
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'webhook_delivery: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: integration_projection
  external_effect: none by default
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: internal
  evidence_kind: server_transactional
  feature_input: ineligible
  retention_class: operational
  required_data: webhook_delivery_id;canonical_event_ids;processed_at
  optional_data: ''
  notes: ''
- event_alias: external.credit.stage.changed
  bounded_context: integrations
  class: integration
  producer: credit_connector
  flow_pattern: external_webhook_ingestion
  authoritative_source: verified_external_source_then_internal_canonical_event
  aggregate: credit_operation
  synchronous_write_set: webhook_receipt; validation_result; normalized_event/outbox
    when valid
  canonical_store: immutable canonical event store
  outbox_required: 'yes'
  ordering: 'credit_operation: aggregate_id + aggregate_version when stateful; otherwise
    event_id/received_at'
  consumers: journey_orchestration;intervention_orchestrator;research_projection
  external_effect: none; event confirms inbound/external result
  retry_policy: bounded exponential backoff with jitter; respect provider retry semantics
  dead_letter: yes for asynchronous delivery/handler failures; rejected commands are
    not DLQ
  reconciliation: integration history + external mapping/checkpoint reconciliation
  privacy_class: restricted
  evidence_kind: external_confirmed
  feature_input: conditional
  retention_class: operational
  required_data: credit_operation_ref;previous_stage_code;new_stage_code;source_occurred_at;source_event_id
  optional_data: business_id;entrepreneur_id;reason_codes
  notes: Contrato reservado; não implementar até receber estados, IDs e fonte oficial
    da operação de crédito.
