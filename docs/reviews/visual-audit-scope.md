# Visual audit scope

The broad crawler covers public, participant and admin routes in desktop and mobile. Critical interaction states that are not discoverable from anchors must have dedicated non-mutating composition checks. The selected participant lesson is one such state because it is opened through a form/server action.

A route or state is not considered visually validated merely because its parent route was captured. The expected state marker must render and its composition must pass the dedicated geometry checks.
