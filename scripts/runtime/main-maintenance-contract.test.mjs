import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import path from "node:path";
import test from "node:test";
import { fileURLToPath } from "node:url";
const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../..");
const read = (relativePath) => readFile(path.join(root, relativePath), "utf8");
test("platform loading uses one global progress bar and no page skeletons", async () => { const [loading, feedback, shell] = await Promise.all([read("apps/web/app/loading.tsx"),read("apps/web/components/navigation-feedback.tsx"),read("apps/web/components/participant-shell.tsx")]); assert.match(loading, /fixed inset-x-0 top-0/); assert.doesNotMatch(loading, /grid-cols|h-48|skeleton/i); assert.match(feedback, /role="progressbar"/); assert.match(feedback, /document\.addEventListener\("submit"/); assert.doesNotMatch(shell, /ParticipantNavigationProgress/); });
test("lesson consumes the full participant content area", async () => { const shell = await read("apps/web/components/participant-shell.tsx"); assert.match(shell, /pathname\.startsWith\("\/empreendedor\/atividade\/"\)/); assert.match(shell, /max-w-none \[&>div\]:max-w-none/); });
test("participant interface preview is administrative and side-effect free", async () => { const [selector, preview] = await Promise.all([read("apps/web/app/admin/experiencia/visual-interface-selector.tsx"),read("apps/web/app/interface-preview/participant/page.tsx")]); assert.match(selector, /\/interface-preview\/participant/); assert.match(selector, /sandbox="allow-same-origin allow-scripts"/); assert.match(preview, /isEstimuloAdministrativeEmail/); assert.match(preview, /InterfacePreviewGuard/); assert.match(preview, /InterfacePreviewBridge/); assert.doesNotMatch(preview, /BehaviorEventTracker|performParticipant|journeyRuntime/); });
test("behavior score validation rejects unsafe configurations at every boundary", async () => { const [editor, migration, action, gateway] = await Promise.all([read("apps/web/components/behavior-score-editor.tsx"),read("supabase/migrations/20260801131203_harden_behavior_score_configuration.sql"),read("apps/web/app/admin/extension-actions.ts"),read("supabase/functions/platform-extensions-rpc/index.ts")]); assert.match(editor, /soma dos pesos/); assert.match(editor, /lacuna/); assert.match(migration, /BEHAVIOR_WEIGHT_TOTAL_INVALID/); assert.match(migration, /BEHAVIOR_CLASSIFICATION_COVERAGE_INVALID/); assert.match(migration, /create trigger trg_behavior_score_configuration_validate/); assert.match(action, /"code" in error/); assert.match(gateway, /domainCode/); });
test("documentation describes the single-journey model", async () => { const [readme, lifecycle, foundation] = await Promise.all([read("README.md"),read("docs/journeys/JOURNEY_LIFECYCLE.md"),read("docs/implementation/APPLICATION_FOUNDATION.md")]); assert.match(readme, /Cada jornada é única/); assert.match(lifecycle, /draft <-> published/); assert.doesNotMatch(foundation, /nova versão.*jornada/iu); });
